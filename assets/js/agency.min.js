/*!
 * Start Bootstrap - Agency v5.2.2 (https://startbootstrap.com/template-overviews/agency)
 * Copyright 2013-2019 Start Bootstrap
 * Licensed under MIT (https://github.com/BlackrockDigital/startbootstrap-agency/blob/master/LICENSE)
 */

(function ($) {
    "use strict";

    // $('a.js-scroll-trigger-fade[href*="#"]:not([href="#"])').click(function(event){
    //     const target = $(this.hash);
    //     if (target.length) {
    //         event.preventDefault();

    //         // Get the background video element
    //         const videoContainer = $('#videoContainer');
            
    //         if (videoContainer.length) {
    //             // Fade out the video
    //             // document.getElementById("page-top").className="fade-out";
    //             // videoContainer.removeClass('fade-in');
    //             // videoContainer.addClass('fade-out');
    //             $('html, body').animate({
    //                 scrollTop: target.offset().top - 54
    //             }, 1000, 'easeInOutExpo');

    //             // Wait for the fade to complete (1s), then scroll
    //             setTimeout(() => {
    //                 $('html, body').animate({
    //                     scrollTop: target.offset().top - 54
    //                 }, 1000, 'easeInOutExpo');
    //             }, 0); // match fade-out duration

    //             // setTimeout(() => {
    //             //     document.getElementById("page-top").className="fade-in";
    //             // }, 1000); // match fade-out duration + scroll duration

    //             // setTimeout(() => {
    //             //     videoContainer.removeClass('fade-out');
    //             // }, 1500); // match fade-out duration + the scroll duration + an extra 250 buffer
    //         } else {
    //             // Fallback if no video element found
    //             $('html, body').animate({
    //                 scrollTop: target.offset().top - 54
    //             }, 1000, 'easeInOutExpo');
    //         }
    //     }
    // });

    // Smooth scrolling using jQuery easing for anchor links with .js-scroll-trigger
    $('a.js-scroll-trigger[href*="#"]:not([href="#"])').click(function () {
        // Check if the link is on the same page
        if (
            location.pathname.replace(/^\//, "") === this.pathname.replace(/^\//, "") &&
            location.hostname === this.hostname
        ) {
            // Get the target section
            var target = $(this.hash);
            target = target.length ? target : $("[name=" + this.hash.slice(1) + "]");

            // Animate scroll if target exists
            if (target.length) {
                $('html, body').animate(
                    {
                        scrollTop: target.offset().top - 54
                    },
                    1000,
                    "easeInOutExpo"
                );
                return false; // Prevent default link behavior
            }
        }
    });

    // Collapse responsive menu when a scroll trigger link is clicked
    $('.js-scroll-trigger').click(function () {
        // $('.navbar-collapse').collapse('hide');
    });

    // Activate scrollspy to add active class to navbar items on scroll
    $('body').scrollspy({
        target: '#mainNav',
        offset: 56
    });

    // Collapse the navbar when page is scrolled
    function navbarCollapse() {
        if ($("#mainNav").offset().top > 100) {
            $("#mainNav").addClass("navbar-shrink");
        } else {
            $("#mainNav").removeClass("navbar-shrink");
        }
    }

    // Collapse now if page is not at top
    navbarCollapse();

    // Collapse the navbar when page is scrolled
    $(window).scroll(navbarCollapse);

})(jQuery);